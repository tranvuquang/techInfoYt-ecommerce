/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  swcMinify: true
  // images: { domains: ["http://localhost:3001"], formats: ["image/png", "image/webp"] },
};

module.exports = nextConfig;
